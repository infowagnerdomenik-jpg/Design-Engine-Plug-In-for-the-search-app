<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.design_engine_example">
    <application
        android:label="design_engine_example"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="flutterEmbedding"
                android:value="2" />
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!-- Showcase Activity for Native Android UI -->
        <activity
            android:name="de.search.dw.search.ui.theme.ui.ThemeActivity"
            android:exported="true"
            android:theme="@style/LaunchTheme">
        </activity>

        <activity
            android:name="de.search.dw.search.ui.theme.ui.FlutterDebugActivity"
            android:exported="true"
            android:theme="@style/LaunchTheme">
        </activity>


    </application>
</manifest>
